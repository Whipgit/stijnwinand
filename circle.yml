machine:
  node:
    version: 6.2.0

dependencies:
  pre:
    - npm install -g npm 

test:
  override:
    - echo "test"
    
deployment:
  staging:
    branch: master
    heroku:
      appname: stijnwinand